<%= form_for(@salesforce_applicant) do |f| %>
  <% if @salesforce_applicant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@salesforce_applicant.errors.count, "error") %> prohibited this salesforce_applicant from being saved:</h2>

      <ul>
      <% @salesforce_applicant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
