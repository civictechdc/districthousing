<%= render 'breadcrumb', object: @employment %>

<%= render 'section_tabs', active_section: :employment %>

<%= render 'progress', object: @employment %>

<%= render 'form' %>

<%= link_to 'Delete', @employment, method: :delete, class: "btn btn-danger", data: { confirm: 'Are you sure?' } %>
