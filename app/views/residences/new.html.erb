<%= bootstrap_form_for(@residence) do |f| %>
  When did you live at this location?
  <%= render "residences/fields", f: f %>
  <%= f.submit 'Add new residence', class: "btn btn-success" %>
<% end %>

<%= link_to 'Back', @current_applicant %>
