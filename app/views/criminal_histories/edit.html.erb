<%= render 'breadcrumb', object: @criminal_history %>

<%= render 'section_tabs', active_section: :criminal_history %>

<%= render 'progress', object: @criminal_history %>

<%= render 'form' %>

<%= link_to 'Delete', @criminal_history, method: :delete, class: "btn btn-danger", data: { confirm: 'Are you sure?' } %>
