<%= bootstrap_form_tag(url: '/household_members') do |f| %>

  Tell us your household member's name:

  <%= f.text_field :first_name %>
  <%= f.text_field :last_name %>

  Or, select a person we already know about:

  <%= f.collection_select(:person_id, @possible_people, :id, :description, include_blank: true) %>

  <%= f.submit 'Create new household member' %>
<% end %>

<%= link_to 'Back', @current_applicant %>
