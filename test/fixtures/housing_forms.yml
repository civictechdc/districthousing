<%
form_counter = 1
Dir.glob(Rails.root.join("public/forms/*.pdf")) do |pdf_path|
%>

form_<%= form_counter %>:
  name: <%= pdf_path %>
  uri: <%= pdf_path %>
  id: <%= form_counter %>

<% form_counter += 1 %>
<% end %>

one:
  name: FakeForm
  uri: FakeUri
  id: 1000

two:
  name: FakeForm
  uri: FakeUri
  id: 2000

unreferenced:
  name: MyString
  uri: MyString
  id: 1001
